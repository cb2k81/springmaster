# Copy .env.example to .env and adapt local credentials.
# This file documents the expected configuration keys for springmaster.

APP_NAME=springmaster
APP_PORT=8080
APP_PROFILE=dev
APP_DB_HOST=localhost
APP_DB_PORT=3306
APP_DEV_DB_NAME=springmaster
APP_STAGE_DB_NAME=springmaster_build
