# Export Tool Bootstrap

The bootstrap export tool supports text exports from `export.config.json`.

Examples:

```bash
./bin/export.sh full
./bin/export.sh full --zip
./bin/export.sh tooling
./bin/export.sh platform
```

The full IDM-derived export tool can replace or extend this implementation in the tooling baseline patch.
