# 000158 springmaster core distribution persistence identity decision

- accepts the Maven-artifact/BOM direction without restructuring the build;
- records nullable assigned-ID newness as the target contract;
- preserves the current eager-zero in-memory runtime until the atomic persistent candidate patch;
- introduces a phase-aware contract check that rejects partial transitions.
