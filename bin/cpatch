#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

if [[ "${1:-}" == "workspace" ]]; then
  shift
  exec "${SCRIPT_DIR}/cocondo-toolkit-launcher.sh" "workspace" "$@"
fi

exec "${SCRIPT_DIR}/cocondo-toolkit-launcher.sh" "patch" "$@"
