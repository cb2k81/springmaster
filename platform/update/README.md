# Platform Update

This directory will contain target descriptors, update rules, generated manifests and templates.

The bootstrap only creates the structure. Patch generation is intentionally not active yet.
